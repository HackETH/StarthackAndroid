<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.noahh_000.starthack">

    <uses-permission android:name="android.permission.INTERNET" />
    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <uses-permission android:name="android.permission.CAMERA" />
        <uses-permission android:name="android.permission.INTERNET"/>
        <uses-permission android:name="android.permission.RECORD_AUDIO"/>
        <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
        <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
        <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Initialization">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HelperIntroActivity"
            android:label="@string/title_activity_helper_intro"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".HelpedPickerActivity" />
        <activity android:name=".HelpedMain" />
        <activity android:name=".CallLoadingActivity" />
        <activity
            android:name=".AcceptingVideoActivity"
            android:label="@string/title_activity_accepting_video"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ConversationActivity"
            android:label="@string/title_activity_conversation"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".ReadyTranslatorActivity"
            android:label="@string/title_activity_ready_translator"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".CallLoadingActivity"></activity>
    </application>

</manifest>
